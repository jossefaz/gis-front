import * as map from "./map";
import * as features from "./feature";
import * as overlays from "./overlay";
import * as layers from "./layer";
import * as interactions from "./interaction";

const API = {
  map,
  features,
  overlays,
  layers,
  interactions,
};

export default API;
